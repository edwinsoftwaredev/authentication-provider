version: v0.5.5-alpha.1

serve:
  public:
    base_url: http://127.0.0.1:4433/
    cors:
      enabled: true
  admin:
    base_url: http://127.0.0.1:4434/

identity:
  default_schema_url: file:///etc/config/kratos/movie-polls-user.schema.json

selfservice:
  flows:
    login:
      ui_url: http://localhost:4455/auth/login
  methods:
    password: # form of authentication
      enabled: true
  default_browser_return_url: http://localhost:4455/ # this is the same. It is just for testing purpose.
  whitelisted_return_urls:
    - http://localhost:4455/

hashers:
  argon2:
    parallelism: 1
    memory: 131072 # 128MB
    iterations: 3
    salt_length: 16
    key_length: 32